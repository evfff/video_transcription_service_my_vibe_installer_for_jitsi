fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
requests==2.32.3
pydantic==2.9.2
PyYAML==6.0.2

# Транскрибация (CPU по умолчанию)
faster-whisper==1.1.1

# Дополнительно для диаризации (опционально, требует DIARIZATION_ENABLED=true)
# pyannote.audio==3.3.2
# torch==2.4.1

# Медиа-обработка (ffmpeg должен быть установлен в системе/образе)
# ffmpeg-python==0.2.0  # только если нужен Python-враппер
